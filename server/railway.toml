# Railway Deployment Configuration

# Use Node.js 20
[build]
  builder = "nixpacks"
  buildCommand = "npm install && mkdir -p dist && npx tsc"
  
[deploy]
  command = "npm start"

# Health check endpoint
[health]
  path = "/"
  interval = 30
